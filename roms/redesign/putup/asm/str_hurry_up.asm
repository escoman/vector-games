; Данные: str_hurry_up
; Адрес: 0x0B01
; Размер: 13 байт (12 симв. + терминатор 0FFh)
; Назначение: предупреждение о времени " HURRY UP ! " (пробелы по краям).
;             Найдено в блоке строк 0x0AC7-0x0B3B; вызывающий код не дизассемблирован.
;             Терминатор — 0FFh.

str_hurry_up:
    DEFM " HURRY UP ! "
    DEFB 0FFh
