; Данные: str_game_over
; Адрес: 0x0B0E
; Размер: 10 байт (9 симв. + терминатор 0FFh)
; Назначение: сообщение о конце игры "GAME OVER". Найдено в блоке строк 0x0AC7-0x0B3B;
;             вызывающий код не дизассемблирован (вероятно, путь исчерпания жизней).
;             Терминатор — 0FFh.

str_game_over:
    DEFM "GAME OVER"
    DEFB 0FFh
